# Kubernetes Cluster Extensions

Interim copies from b-ops. These files provide Helm chart versions and
values for Cilium (CNI) and OpenEBS (storage) deployment via ansible.

They will be replaced once `business-operations` exposes these components
as reusable modules.
